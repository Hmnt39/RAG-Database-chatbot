# RAG-based Application

A retrieval-augmented generation (RAG) based CLI chatbot with MongoDB integration. This chatbot combines document retrieval with LLM-based question answering to provide context-aware responses.

